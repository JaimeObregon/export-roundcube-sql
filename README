Script para facilitar la migración de datos de Roundcube de un dominio.
Véase ticket #1012872.

// Vaciar cache?
// sanitize
// idempotencialidad


SELECT * FROM users WHERE username LIKE '%@dominio.com';

	SELECT * FROM identities WHERE user_id = '';

	SELECT * FROM contactgroups WHERE user_id = '';

	SELECT * FROM contacts WHERE user_id = '';

		SELECT * FROM contactgroupmembers WHERE contact_id = '';


users
identities -> user_id
contacts -> user_id
contactgroups -> user_id
contactogroupmembers -> contactgroup_id, contact_id
